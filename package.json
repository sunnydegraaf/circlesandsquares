{
  "name": "circlesandsquares",
  "version": "1.0.0",
  "description": "game for CLE4",
  "main": "index.js",
  "scripts": {
    "build": "parcel build src/index.html --public-url ./ --out-dir docs --no-source-maps",
    "watch": "parcel watch src/index.html --out-dir docs",
    "start": "parcel serve src/index.html --open --out-dir docs",
    "clean": "rm -v docs/*"
  },
  "author": "thomasgertenbach",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.0.2",
    "parcel-bundler": "^1.10.3",
    "typescript": "^3.4.5"
  },
  "dependencies": {
    "papaparse": "^5.2.0",
    "parcel": "^1.12.3",
    "phaser": "^3.18.0"
  }
}
